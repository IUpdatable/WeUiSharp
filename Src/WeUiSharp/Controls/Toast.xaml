<Window x:Class="WeUiSharp.Controls.Toast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeUiSharp.Controls"
             mc:Ignorable="d" 
             WindowStyle="None"
             SizeToContent="WidthAndHeight"
             AllowsTransparency="True"
             Background="Transparent"
             WindowStartupLocation="CenterOwner"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border CornerRadius="2" Background="Black" Opacity="0.575">
        </Border>
        <TextBlock Foreground="{DynamicResource Toast.Static.Foreground}"
                   Margin="10"  Opacity="1"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Message}" />
    </Grid>
    
</Window>
